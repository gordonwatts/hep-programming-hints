apiVersion: v1
kind: Service
metadata:
  name: {{ include "hep-programming-hints.fullname" . }}
  labels:
    {{- include "hep-programming-hints.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hep-programming-hints.selectorLabels" . | nindent 4 }}